# 小月亮 (Little Moon) 修改记录

## 1.17.6 - 2026-08-15

### 修改
- **怪物强化** — 「娱乐」难度选项移除，改为独立配置项「怪物附魔几率」(`MOB_ENCHANT_CHANCE`)：难度选项恢复 简单/普通/困难/噩梦；新增几率选项 10%/25%/50%/75%/100%（默认 100%，与原行为兼容），几率与难度解耦——通过几率判定后仍按所选难度抽取附魔数
  - 涉及文件：`modinfo.lua`、`scripts/core/config.lua`、`scripts/features/mob_enhance/init.lua`
- **怪物强化** — 附魔详情显示：21 个怪物附魔新增 `desc` 描述字段，HH 面板与检查文本由仅显示附魔名字改为逐行显示「附魔名：详情」（按获得顺序 `_enchant_order`，替代原 `pairs` 随机顺序）
  - 涉及文件：`scripts/features/mob_enhance/enchants_pool.lua`、`scripts/components/moon_mob_enhance.lua`
- **怪物强化** — 标签简化：`[月之强化]`/`[月之首领]` 统一改为 `月化`（去掉方括号，Boss 与普通怪一致，HH 面板仍以标题颜色区分）
  - 涉及文件：`scripts/components/moon_mob_enhance.lua`

## 1.17.5 - 2026-08-12

### 新增
- **物品禁用天数限制** — `BAN_ITEMS` 配置支持 `'prefab|N'` 格式：`'prefab'` 永久禁用；`'prefab|30'` 表示禁用 30 个游戏日后自动解禁（起始天数 = 配置生效时，随世界存档持久化，服务器重启不重置）。到期后自动恢复物品生成与配方（含原版配方的 canbuild 还原）
  - 涉及文件：`scripts/features/ban_items.lua`、`scripts/components/moon_ban_timer.lua`（新增）、`modinfo.lua`

### 修复
- **物品禁用启动崩溃** — 天数解析在 modimport 加载阶段调用裸 `tonumber`，沙箱环境中为 nil（strict 服务器直接崩溃，报错 `attempt to call global 'tonumber' (a nil value)`）。改为 `_G.tonumber`（加载阶段数字转换一律走 `_G.`，与 treasure.lua/quick_dig.lua 先例一致；运行时回调可用裸函数）
  - 涉及文件：`scripts/features/ban_items.lua`
- **物品禁用运行崩溃** — `moon_ban_timer` 组件 `GetStartCycle` 引用裸 `GLOBAL`：组件由引擎 `require` 加载（非 modimport 沙箱），strict 模式下 `GLOBAL` 未声明，世界加载后首个任务帧直接报 `variable 'GLOBAL' is not declared`。改为 `self.inst.state.cycles`（组件即挂在 TheWorld 上，无需全局；与 moon_mob_enhance 组件注释"require 加载 GLOBAL 不可用"同坑）
  - 涉及文件：`scripts/components/moon_ban_timer.lua`
- **开局礼包** — 修复跨世界重复领取：已领记录原先只存 world 组件（随世界存档），地表/洞穴是两个独立 world 且各持独立 user session，玩家进洞穴（尤其退出重进）又能领一次。改为**集群共享文件**为权威：已领记录写入所有 shard 进程都能访问的同一份 TheSim 持久化文件（候选位置按序探测：主机模式 `InClusterSlot(slot,"Master")` → 专用服务器 `"../../"` 相对路径上跳至 cluster 根 → 逐级兜底，首个可读位置即共享位置，启动时打印 `[小月亮] 开局礼包共享存储位置` 便于排查）；**重置清零**：共享文件不随世界重置删除，故额外记录每个 shard 的 `session_identifier`（key 用 `TheShard:GetShardId()`，地表 "1"/各层 "2"/"3"…，支持多层世界），某 shard session 变化即判定该世界重建 → 已领记录作废 → 重置后可重新领取；world 组件仅作旧档兼容（老版本记录查询时同步进共享文件）；**数量修复**：发放改为逐个数生成，配置给多少就给足多少，不再受物品堆叠上限（maxsize）截断影响（此前 `math.min(count, maxsize)` 在堆叠上限被环境改变时会少发）
  - 涉及文件：`scripts/features/start_gift.lua`

### 修改
  - 去掉堆叠相关代码

## 1.17.4 - 2026-08-11
### 修改
  - 去掉新史低 加上等等秋零和 小野小和组合技  

## 1.17.3 - 2026-08-08

### 新增
- **云中雀** — 新增被动暴击：攻击有 15% 几率造成 300% 伤害（暴击 3 倍；HH 框架 `criticalHitRate` 15 + `criticalHitEffect` 100，翱翔 450% 范围伤害不受影响）
  - 涉及文件：`scripts/enchants/yzq.lua`

### 修改
  - Boss强化去掉养猫客  

## 1.17.2 - 2026-08-07

### 修复
- **召唤群友（附魔）** — 修复 `sleeper:SetSleepiness` 调用不存在的 API（`sleeper` 组件无此方法，夜晚召唤/装备触发时直接报错崩溃）：改为 `SetSleepTest(function() return false end)`；顺带清空召唤猪人的掉落物（`SetLoot({})` + `ClearRandomLoot()`），杀猪不掉肉/猪皮
  - 涉及文件：`scripts/enchants/qunyou.lua`、`scripts/features/moon_qunyou.lua`
- **新史低** — 修复服务端折扣"有时生效有时不生效"的加载顺序竞态：`moneymanager` 是欧皇模拟器在它自己的 `AddPlayerPostInit` 回调里动态添加的组件（非 prefab 静态注册），而 `AddPlayerPostInit` 回调按 mod 加载顺序执行——本 mod 回调先跑时组件还不存在，原一次性检查直接跳过 OnBuy 包装且不重试，导致服务端扣款/余额判断永远走原价（客户端 UI 却显示折后价，典型症状：host 生效、联机队友不生效）
  - 修复：OnBuy 包装即时尝试一次，失败则 `inst:DoTaskInTime(0, ...)` 延迟兜底重试（所有 `AddPlayerPostInit` 回调在同一帧内执行完，下一帧组件必定已加），保持"调用链最外层"不变
  - 涉及文件：`scripts/enchants/xinshidi.lua`

### 修改
- **新史低** — 恢复加载（1.17.1 因上述问题暂时去掉）：取消 `modmain.lua` 中 `modimport("scripts/enchants/xinshidi")` 注释
  - 涉及文件：`modmain.lua`
- **心平气和 / 千月野** — 由娱乐附魔重做为「攻守兼备」套装：心平气和=防御位（单件减伤10%+每3秒回1.5%生命；与千月野齐穿→减伤25%、回复翻倍、受击30%几率获得25%生命护盾，冷却8秒），千月野=输出位（单件伤害+12%、攻速+10%；与心平气和齐穿→伤害+30%、攻速+30%、击杀触发月爆250%范围伤害，冷却3秒）
  - 套装判定用 `Moon_HasEffect` 双向检查（HH 原生套装机制已被作者硬编码禁用，`hh_player:HasSuitEffect` 恒 false，故自实现组合技）
  - 同名同 ID（`Legend_XPING` / `Legend_QIANYUE`），旧娱乐代码已删除；掉落权重保持 0.01
  - 涉及文件：`scripts/enchants/xping.lua`、`scripts/enchants/qianyue.lua`、`docs/enchants.md`、`steam_description.txt`
- **云中雀** — 恢复加载（此前暂时注释，待重新启用）：取消 `modmain.lua` 中 `modimport("scripts/enchants/yzq")` 注释
  - 移除翱翔范围伤害的击退效果，改为纯数值强化：350% → 450% 攻击力范围伤害
  - 修复翱翔无敌 DoDelta 包装：改为保存 wrapper 句柄并按归属校验还原（避免与其他 DoDelta 附魔同穿时覆盖包装链），无敌期间伤害归零走原函数（保持返回语义），1.5s 任务存句柄卸载时 Cancel
  - 涉及文件：`scripts/enchants/yzq.lua`、`docs/enchants.md`（#11 描述同步为当前实现）
- **等秋零** — 去掉蕨叶效果

### 新增
- **召唤群友（月亮商店）** — 商店新增「召唤群友」兑换（100 水晶小人，猪人图标 `pig_man`）：制作后原地召唤 1 只猪人群友（各有名字+登场对白，跟随打架，不睡觉，击杀无掉落），玩家周边最多同时 3 只，存活 5 分钟、每 60 秒补员 1 只（有空位才补，补到 3 只上限为止）；满员重复兑换不再出新并提示
  - 实现：新 prefab `moon_qunyou_summon`（瞬发召唤实体，利用制作流程 `onbuilt` 事件直接定位制作玩家）；新配置项 `ENABLE_MOON_SHOP_BOSS_QUNYOU`（默认开启，需 HH 附魔模组）
  - 涉及文件：`scripts/features/moon_qunyou.lua`（新）、`scripts/features/moon_shop.lua`、`scripts/core/config.lua`、`modmain.lua`、`modinfo.lua`、`images/inventoryimages/pig_man.tex/xml`（新）

## 1.17.1 - 2026-08-04

### 删除
- **新史低** — 暂时去掉

## 1.17.0 - 2026-08-03

### 新增
- **开局礼包** — 玩家进服后可自选领取一次开局礼包（每个世界仅一次，已领记录随世界存档持久化，重新生成世界自动重置）
  - 配置：`ENABLE_START_GIFT` 总开关 + `START_GIFT_PLANS` 单一配置串，格式 `物品,数量,角色|物品,数量,角色:::方案2`（`:::` 分方案、`|` 分同方案物品、角色 `all`=所有人 或 角色prefab 定向；数量/角色可省略，默认 1/all；方案数自动决定，标签自动生成"礼包A/B/C..."）
  - 交互：物品栏上方「开局礼包」按钮 → 面板弹窗（待办列表样式：方案名 + 完整礼包内容 + 「领取」按钮）→ 专属物品标注 `(角色名)`（如 `金块 x5(wilson)`）→ 点击领取打包成 `gift` 礼盒开袋获取
  - 涉及文件：`scripts/features/start_gift.lua`（新增）、`scripts/features/start_gift_client.lua`（新增）、`scripts/components/moon_start_gift_store.lua`（新增，OnSave/OnLoad 持久化已领记录）、`modinfo.lua`、`scripts/core/config.lua`、`modmain.lua`

### 修复
- **新史低** — 折扣数值映射反转：附魔石属性值越高折扣越好（9=满值→5折，5→9折，原实现 5→5折/9→9折 与一般附魔"数值越大越强"直觉相反）
  - HH 词条描述同步换算：包装 `hh_equip:GetBuffDebugList`，把描述中的折数替换为映射后值，满值石头正确显示「商店物品5折(已满)」而非误导的「9折(已满)」
  - 涉及文件：`scripts/enchants/xinshidi.lua`

## 1.16.2 - 2026-08-02

### 修改
- **有机蔬菜** — 吃锅料理效果改为 100% 双倍（血量/饥饿/理智回复×2），并新增 20% 几率返还一个（等同不消耗）
  - 涉及文件：`scripts/enchants/youjishucai.lua`（返还几率常量 `RETURN_CHANCE = 0.2` 可调）

### 修复
- **新史低** — 修复部分商店物品不享受折扣：`moneymanager.OnBuy` 包装移至 `AddPlayerPostInit`（晚于所有 `AddComponentPostInit`，确保在调用链最外层），新增「实际扣款 − 折后总价」差额退还，「黑店」等绕过 `getShopItemFinalPrice` 的写死价扣款也按折后价净扣
  - 涉及文件：`scripts/enchants/xinshidi.lua`
- **新史低** — 装备描述补充说明：装备后需重新打开商店才生效（欧皇商店价格在打开/翻页时渲染，不实时刷新）

## 1.16.1 - 2026-07-31

### 修改
- **怪物强化 — 反伤** — 修复反伤档时候 免疫反伤无效的问题

## 1.16.0 - 2026-07-31

### 修改
- **怪物强化 — 附魔数量调整** — 取消「基础 + 额外」叠加机制，Boss 与普通怪统一按难度抽取固定数量：简单 1 / 普通 3 / 困难 5 / 噩梦 7 个附魔
  - 涉及文件：`scripts/features/mob_enhance/init.lua`（`DIFFICULTY` 表 `extra_enchants` 改为 `enchant_count`，抽取逻辑直接取难度数量）、`modinfo.lua`（难度选项描述与 hover 文案同步更新）

## 1.15.9 - 2026-07-31

### 新增
- **附魔「新史低」** — 新增附魔 新史低（词条 ID `Legend_XSD`）：需同时开启「欧皇模拟器」(workshop-3273001012) 才生效；装备后在欧皇模拟器商店购物打 5~9 折（附魔石属性值 5~9 随机，仅装备者本人享受折扣）；精英/Boss 低权重掉落（0.005）
  - 折扣走 `slotmachineutils.getShopItemFinalPrice` 单点 hook：服务端购买扣款、客户端商店 UI 显示、余额预估三处价格一致
  - 双端注册：服务端 `AddPrefabPostInit("world")`，客户端 `AddPlayerPostInit` 补装词条 + hook，避免 UI 显示原价/余额误判
  - 涉及文件：`scripts/enchants/xinshidi.lua`（新增）、`modmain.lua`（注册加载）

### 修改
- **养猫客** — 暂时停用「报恩」效果（浣猫每 30 秒捡物、5% 概率带鱼），召唤/属性加成/牺牲挡刀/夜眼不受影响
  - 涉及文件：`scripts/enchants/yangmaoke.lua`
- **新史低** — 装备词条描述由区间改为显示具体折扣值（`%s` 占位符，如"商店物品5折"），帮助面板仍显示 5~9 折区间

### 修复
- **新史低** — 修复 `strict.lua` 严格模式下读取未声明全局变量 `_Moon_ShopBuyer` 导致购买崩溃：临时"购买上下文"全局变量改用 `rawset`/`rawget` 读写（详见 `docs/MODDING_PITFALLS.md` 第 20 条）

## 1.15.9 - 2026-07-26
### 修改
- **小月亮商店** — `ENABLE_MOON_SHOP_BOSS` 拆分为四个独立配置项：`ENABLE_MOON_SHOP_BOSS_CELESTIAL`（天体后裔）、`ENABLE_MOON_SHOP_BOSS_STALKER`（织影者）、`ENABLE_MOON_SHOP_BOSS_ALTERGUARDIAN`（天体英雄）、`ENABLE_MOON_SHOP_BOSS_CRABKING`（帝王蟹），可单独开关

### 修改
- **功能** — 暂时隐藏堆叠和物品吸入

### 新增
- **怪物强化模块** — 独立的 Boss/普通怪物强化系统，与现有附魔体系完全解耦
  - 34 个 Boss（龙蝇/巨鹿/天体英雄/晶体巨鹿等）+ 50+ 种普通敌对生物可获得强化
  - 22 个怪物版附魔（从现有附魔适配），Boss 抽 3~8 个，普通怪抽 2~7 个
  - 防御层（可配置）：基础减伤/动态减伤/单次限伤/受伤限频/防御范围，默认关闭
  - 配置项：`ENABLE_MOB_ENHANCE` / `ENABLE_MOB_DEFENSE` 等 6+1 个配置
  - HH 面板显示 `[月之首领]` 或 `[月之强化]` 信息；非 HH 模式检查文本显示
  - 检测到让我瞧瞧 mod (3700206644) 时自动跳过防御层，避免冲突

### 新增
- **小月亮商店** — 新增 Boss 兑换：天体英雄 (`alterguardian_phase1`) 和 帝王蟹 (`crabking`)，各 100 水晶小人兑换

### 修复
- **月半/萝的守护** — 免疫僵直无效的问题
  - 根因：之前只通过 `immunityKnockBack` 免疫了击退，但未处理 DST 的受击动画僵直（attacked stategraph 事件）
  - 修复：在核心层添加 `AddStategraphPostInit('wilson', ...)` 钩子拦截 attacked 事件，附魔装备时注册 `jiangzhi` 效果
  - 涉及文件：`mod_utils.lua`（全局钩子）、`yueban.lua`、`luo.lua`（注册/移除 jiangzhi 效果）

### 新增
- **蔷薇主教** — 新增附魔 蔷薇主教：将血肉织成能抵御伤害的披风。血量≤50%时每损失1%血+0.6%移速（上限+30%）；受击冰霜爆发（4格内敌人冻结1秒+50%攻击力伤害，CD4秒）；6格内有队友时双方缓慢回血
  - 涉及文件：`scripts/enchants/qiangwei.lua`、`modmain.lua`（注册加载）

- **伊普西隆** — 新增附魔 伊普西隆：数学概念「极限累积」。每次攻击叠加 1 层印记（上限 5 层）；每层印记本次攻击额外造成目标当前生命值 0.5% 的真实伤害（先叠层再结算：第1击 0.5% → 第5击 2.5% 叠满）；5 层满时下一次攻击造成目标最大生命值 3% 的真实伤害，随后清空印记重新累积
  - 掉落：精英/Boss 死亡 1% 概率掉落（词条ID `Legend_EPSILON`）
  - 涉及文件：`scripts/enchants/epsilon.lua`、`modmain.lua`（注册加载）

### 修改
- **紫蝶** — 蝶影不再召唤随机怪/大虚影，改为启迪之冠小虚影 `alterguardianhat_projectile`（像启迪之冠一样飞向玩家当前目标攻击，继承50%属性，最多2个）
  - 修复虚影没有紫色阴影的问题：虚影可见主体是客户端单独 spawn 的 blobhead 大头，需同时染主实体+大头，并替换 transparentonsanity 的 onalphachangedfn 防止被刷白
  - 双保险确保虚影绝不攻击玩家（leader 盟友判定 + find_attack_victim 过滤 player tag）
  - 涉及文件：`scripts/enchants/zidie.lua`
- **蝴蝶的小阿飞** — 全面强化：护体蝴蝶 3→5 只，受击从全额抵消改为消耗1只减免60%伤害；恢复 8秒→6秒，击杀回复 15→25血+10→15精神并恢复2只；新增每只蝴蝶+4%伤害（满5只+20%），随消耗/恢复实时增减；移速+20%保留；移除蝶影弹（与紫蝶重复）
  - 涉及文件：`scripts/enchants/hufei.lua`

### 新增
- **等等秋零** — 新增附魔 等等秋零：我是浅笑我怕谁！暴击率+30%、暴击效果+100%（暴击3倍）；每次攻击附带50点真实伤害（无视防御穿刺）；真伤可被"破虚"附魔转换（每50点→+5%暴击率+10%暴击效果），与"等秋零"（防御免疫）并存
  - 涉及文件：`scripts/enchants/dengdengqiuling.lua`、`modmain.lua`（注册加载）

### 修复
- **蓝图掉落报错** — 掉落任意蓝图随机抽到小月亮商店配方时，原版 `blueprint.lua:201` 拼接 nil 崩溃
  - 根因：商店配方 `AddRecipe2("MoonShop_xxx", ...)` 的 recipe.name 形如 `MoonShop_cutstone`（非产物 prefab 名），`STRINGS.NAMES` 无对应条目
  - 修复：modmain 新增"蓝图本地化兜底"，遍历 `AllRecipes` 为所有缺本地化名字的配方补兜底名（`MoonShop_*` 显示"小月亮商店兑换"，其他 mod 配方用原名兜底），零副作用
  - 涉及文件：`modmain.lua`
- **蔷薇主教** — 三处修复：
  - 持续伤害（寒冷/火焰/中毒等 `overtime`）不再触发冰霜爆发，只对战斗打击生效
  - `GLOBAL.FindEntities` 在严格模式（strict.lua 未声明）下不可用 → 改用 C 层 `TheSim:FindEntities`（与全 mod 惯例一致）
  - 玩家侧 `combat:DoDamage` 在 HH 框架下为 nil → 改用目标侧 `combat:GetAttacked`
  - 涉及文件：`scripts/enchants/qiangwei.lua`
- **蝴蝶的小阿飞** — 持续伤害不再消耗护体蝴蝶（冷/火/中毒环境护盾不再被秒清空）；新增可见的紫色光翼蝴蝶实体绕身环绕，数量与护体数实时同步（受击减、恢复/击杀加），卸载时全部清除
  - 涉及文件：`scripts/enchants/hufei.lua`

## 1.15.8 - 2026-07-23
### 新增
- **小月亮商店** — 新增月熠兑换：5 个月亮碎片 → 1 个月熠，配置项 `ENABLE_MOON_SHOP_SPARK` 控制
- **麻辣兔头** — 新增附魔 麻辣兔头：+15%移速/战斗+22%移速/低血瞬移留假身/采集3%多一份/1%概率822%暴击/惊喜种子
- **养猫客** — 新增附魔 养猫客：每60秒召浣猫(上限3)/每只+8%攻速+10%移速/猫挡刀(180s)/捡物报恩5%带鱼/夜眼

### 修改
- **小月亮商店** — 默认面板折叠

## 1.15.7 - 2026-07-19

### 修复
- **胖虎** — 魔音贯耳不再对队友（其他玩家）造成友伤
- **毛旭** — 修复脱穿装备血量叠加 bug：每件装备改用独立 source key（`inst.GUID`），`un_equip_fn` 用效果总量差值控制扣血，只有当 `ReduceEffect` 实际减少计数时才减 maxhealth，杜绝跨会话/事件重复导致的血量异常叠加

## 1.15.6 - 2026-07-17

### 修复
- **胖虎、良弓藏** —  会打到物品栏小动物的问题
- **草莓奶昔** —  给草莓奶昔附魔加了服务端守卫（修掉客户端双跑导致的假死）、把重型特效换成轻量版、补上周期 Task 的 IsValid 保护、精神回复从 1% 改回 3%，并真正实现了被攻击时给敌人 -35% 攻速移速的"糖分过量"减速。

## 1.15.5 - 2026-07-12

### 新增
- **寻宝卷轴兑换** — 简化兑换档位：1 卷轴(50金) / 10 卷轴(500金)`
- **兔兔生财** — 新增附魔 兔兔生财，好运滚滚来

## 1.15.4 - 2026-07-08

### 新增
- **小月亮商店** — 检测到模组 `2526778484` 时，增加灵魂互换配方：3 暗影之魂 → 1 光明之魂 / 3 光明之魂 → 1 暗影之魂
- **新增配置** — `ENABLE_MOON_SHOP_BATCH`(精炼材料批量兑换/彩虹宝石)、`ENABLE_MOON_SHOP_BOSS`(Boss掉落物兑换)、`ENABLE_MOON_SHOP_SOUL`(灵魂互换)
- **自动堆叠** — 增加冲突检测，检测到下划线堆叠模组(3253273657)已加载时自动跳过，避免双重生效
- **锁定跑速** — 防客户端加速 mod（如 Fast moving 修改 `TUNING.WILSON_RUN_SPEED`），客户端/服务端定时复位 `locomotor.runspeed`，配置项 `LOCK_RUN_SPEED` 默认开启
- **遍历之迹兑换** — 小月亮商店用 500 水晶小人兑换遍历之迹，配置项 `ENABLE_MOON_SHOP_TRAVEL_TRACES` 控制
- **新附魔：月半** — 免疫击退/僵直，攻击附带自身当前生命值 5% 额外伤害，受击 20% 几率「肉弹冲击」：周围 3 码敌人 150% 伤害 + 减速 30% 持续 3 秒，掉落率 1%
- **modinfo 重组** — "客户端换人控制"移入"客户端反作弊"分组，与锁定跑速并列

### 重构
- **小月亮商店** — `demon_altar.lua` 合并到 `moon_shop.lua`，所有配方统一走 `AddRecipe2` + `filter_list`
  - 恶魔祭坛(emojitan)、世纪之花球茎(shijizhihua_bulb)移至商店标签页
  - Boss 兑换（天体后裔/织影者）、灵魂互换（光明之魂/暗影之魂）配方接入配置独立控制

### 修改
- **劳动最光荣** — 关闭打怪掉落（掉落权重改为 0），仅保留吃烤土豆保底获取
- **配置调整** — modinfo 重新分组排序，堆叠上限选项增加"请勿同时开启多个堆叠上限模组"提示

### 修复
- **贴图修复** — 月亮商店 Boss/遍历之迹/祭坛/球茎 贴图 & 逻辑修复


## 1.15.3 - 2026-07-03

### 修复
- **死亡统计** — 换人后死亡计数清零的 bug，计数改为按 userid 持久化，换人自动恢复
- **自动堆叠** — 物品处于 LIMBO 状态时 `IsOverStacked` 对 nil 和数字比较崩溃，加 nil 守卫直接返回 false

### 新增
- **死亡统计** — 配置项 `换人重置计数`，默认不清零，可选换人后清零


## 1.15.2 - 2026-07-01

### 优化
- **面板** — 小月亮面板显示条件修改

### 修改
- **附魔石掉落** — 重构为两级 roll 机制，总掉率固定 0.5%（之前 ~29%），每次最多掉 1 个


## 1.15.1 - 2026-06-30
### 优化
- **面板** — 部分设置默认不开启


## 1.15.0 - 2026-06-30

### 新增
- **小月亮商店** — 制作栏新增独立标签页，批量兑换精炼材料
  - 石砖 x10 ← 岩石 x30、木板 x10 ← 木头 x40、绳子 x10 ← 草 x30、莎草纸 x10 ← 芦苇 x40
  - 零外部依赖，纯原版 DST API
  - 配置项 `ENABLE_MOON_SHOP`，默认开启
- **掉落自动堆叠** — 物品掉落时自动与周围同类物品合并堆叠（参考 3253273657_下划线）
  - 扫描半径 20 码，同 prefab + 同 skin 自动合并
  - 排除标签：`smallcreature` / `heavy` / `trap` / `NET_workable`
  - 特殊场景过滤：poop 附近有牛/大象时不堆叠，鸟蛋附近有企鹅时不堆叠
  - 物品被捡起时自动取消待执行的堆叠任务
  - 配置项 `ENABLE_AUTO_STACK`，默认开启
- **堆叠上限** — 修改物品堆叠上限（参考 3253273657_下划线）
  - 统一修改 `STACK_SIZE_SMALLITEM` / `MEDITEM` / `LARGEITEM` / `TINYITEM` / `PELLET`
  - 选项：关闭 / 60 / 99 / 128 / 200 / 500 / 999 / 9999，默认 200
  - 客户端 `stackable_replica.MaxSize` / `SetMaxSize` 同步修正显示
  - 配置项 `STACK_SIZE_MULTIPLIER`，默认 200
  - **注意**：请勿同时开启多个堆叠上限模组
- **更多堆叠** — 让原本不可堆叠的生物/物品也能堆叠（参考 3253273657_下划线）
  - 按 prefab 添加：暗影心房、眼球塔、格罗姆翅膀、牛角、高鸟蛋、岩浆虫卵、恶液套件、告密的心 等
  - 按 tag 添加：鸟(`bird`)、小动物(`smallcreature`)
  - 选项 2 时小动物丢地上会自动逐个分离
  - 需要堆叠上限开启才生效
  - 配置项 `ENABLE_MORE_STACKING`，默认开启

### 修改
- **后裔不会猝死** — 修复后裔单位异常死亡问题

## 1.14.10 - 2026-06-29

### 优化
- **死亡统计面板** — 刷新提示移至标题栏 "死亡统计(每15秒自动刷新)"，内容区域上移，更紧凑

### 新增
- **物品禁用** — 支持禁用指定物品（含 Mod 物品），禁用的物品无法制作，已存在的会被移除并退还材料
  - 配置项 `BAN_ITEMS`，在 mod 配置中添加 prefab 名称列表


## 1.14.9 - 2026-06-28

### 新增
- **死亡统计面板** — 独立浮动窗口，追踪并展示所有玩家的死亡次数排行榜
  - 小月亮助手面板底部新增"死亡统计"区域，点击"打开死亡统计"按钮显示浮动面板
  - 显示前 10 名，格式 `1. 玩家名: N次`
  - 面板每 5 秒自动刷新（仅可见时），支持手动刷新
  - 可拖动，位置持久化保存
  - 由 `ENABLE_DEATH_STATS` 配置控制（默认开启）

### 新增配置
- `ENABLE_DEATH_STATS` — 死亡统计开关，默认开启

### 修复
- **咕咕咕附魔石** — 修复闪避叠加暴击属性的bug（骑之意志）
  - 闪避成功不再重复叠加暴击率/暴击伤害，改为刷新6秒计时
  - 卸下附魔时无条件清理暴击属性，不清残留
- **毛旭附魔石** — 修复重进存档0血假死
  - 改用 HH 框架标准 `SetPercent` 逻辑，不保存血量绝对值
  - 穿戴/卸下均保持血量百分比不变，杜绝存档恢复时的时序问题

## 1.14.8 - 2026-06-27

### 新增
- **附魔：小野的加护** — 联动魔卡少女小樱mod，仅小樱角色可用，仅可附魔在卡牌盒上
  - Boss血量≤10%时锁血4秒（同目标10秒冷却），满级卡牌盒1.5%秒杀

### 修改
- **紫蝶分身概率** — 20%→5%
- **快捷发送重做** — TextEdit 输入框改为预设消息按钮（左键发送，右键编辑），编辑时推 Screen 弹窗，彻底解决键盘穿透导致 WASD/技能键误触人物的问题

## 1.14.7 - 2026-06-26

### 新增
- **快捷换装** — 物品栏上方显示"换装"按钮，随时随地打开更衣室（`ENABLE_WARDROBE_ANYWHERE` 配置控制，默认关闭）
- **快捷发送提示** — 助手面板快捷发送输入框上方增加操作提示文字

### 修改
- **皮肤通用化** — 暂时注释（未完成，下个版本继续）

### 修复
- **胖虎附魔崩溃** — `panghu.lua` DoDelta 的 cause 参数从 entity 改为字符串 `"panghu"`，避免 worldoverseer JSON 序列化 entity 时遇到非字符串 key 导致 `gsub` 崩溃

## 1.14.6 - 2026-06-23

### 新增
- **附魔：幸运橙汁** — 词条ID `Legend_XYCZ`（#31）
  - 击杀敌人20%额外掉落、采集15%双倍产出、收锅20%多一份
  - 幸运值+10（`Moon_AddEffect("luck", ...)`，其他系统可查询）
  - 制作物品15%概率不消耗材料（仅材料，角色消耗正常扣除）
  - 🎲 **获取方式**：面板"幸运骰子"掷骰子，连续两次掷出80+获得附魔石（期望~27次），不通过Boss掉落
- **骰子系统**：助手面板新增"幸运骰子"模块
  - 点击"掷骰子"按钮 → 服务端生成随机数（防作弊）
  - 显示上次/本次点数，80+橙色高亮，成功金色提示
  - 5秒冷却，上次投掷记录随存档持久化
  - 由 `ENABLE_MORE_ENCHANTS` 配置项控制开关
- **附魔：七步之内（小乖）** — 词条ID `Legend_XIAOGUAI`（#32）
  - 攻击速度+100%（HH框架2倍上限），仅小樱(ccs)可用
  - 仅限魔杖(`ccs_magic_wand3`)附魔，其他武器不可附魔
  - 掉落：Boss 1%概率

### 修改
- **七步之外** → **七步之外**：附魔更名，`client_text` 改为 `七\n步`
- **幸运橙汁** (`xingyunchengzhi.lua`)：#roll 冷却 15 秒 → 10 秒

### 修复
- **劳动最光荣** (`laodong.lua`)：关闭 `ENABLE_MORE_ENCHANTS` 后计数钩子仍运行的bug
  - 根因：`AddPrefabPostInitAny` 和 `stewer` 计数部分在CFG守卫之前注册
  - 修复：将 `if not CFG.ENABLE_MORE_ENCHANTS then return end` 提前到所有钩子之前

### 修改
- **附魔描述精简**：精简 15 个附魔的 `desc` 字段，解决帮助面板"词条属性"中长描述相互覆盖的问题

## 1.14.5 - 2026-06-22

### 新增
- **附魔：山竹的捏** — 词条ID `Legend_SHANZHU`（#30）
  - 厚皮甜心 — 战斗护盾吸收25%最大生命，护盾存在时移速+15%、攻击+20%
  - 破盾回复+果香迸发AoE，8秒再生
  - 每60秒「果中皇后」5秒：护盾翻倍，攻击附带50%真实伤害
  - 打怪15%概率双倍掉落
  - 小樱角色(ccs)每日天亮送两份双皮奶
  - 掉落：精英/Boss 死亡 1% 概率掉落

### 修复
- **劳动最光荣** (`laodong.lua`)：修复多项计数失效
  - 采集草/树枝不计数：`picksomething` 的 `data.pickable` 对野草等为 nil，缺少回退分支
  - 巨大作物不计数：`finishedwork` 只监听了 MINE/CHOP/BUILD，漏了 HAMMER+农场植物
  - 锅做饭计数不稳定：两个 `stewer` postinit 互相独立，合并为一个组件钩子
- **劳动最光荣** (`laodong.lua`)：描述中"巨大作物50"改为"敲碎巨大作物50"，避免混淆

## 1.14.4 - 2026-06-22

### 修复
- **劳动最光荣** (`laodong.lua`)：修复采摘时 `picksomething` 事件中 `data.loot` 为 Lua 表时调用 `IsValid()` 导致的崩溃
  - `data.loot` 可能是多个战利品的列表（Lua 表）而非实体 userdata，表没有 `IsValid` 方法
  - 修复：先检查 `type(loot)`，只有 userdata 才调用 `IsValid()`

## 1.14.3 - 2026-06-22

### 修改
- **劳动最光荣** (`laodong.lua`)：重做获取方式 + 新增进度播报
  - 旧：吃135个烤土豆保底获得
  - 新：挖矿/砍树/采集作物/烹饪/制作各300 + 采集巨大作物50
  - 每50次（巨大作物10次）播报一次进度
- **七步之外** (`changpi.lua`)：攻击距离数值由附魔石生成时确定（2/3/4，4为满级），新增 `value_range = {2,4}`，修复 desc 中 `%` 转义导致显示异常

### 优化
- **胖虎** (`panghu.lua`)：移除魔音贯耳的烟雾/爆炸特效（`sonicresonate_fx` / `collapse_small`），连击时频繁生成粒子特效导致严重掉帧
- **良弓藏** (`lianggongcang.lua`)：自身伤害从 -100% 改为 -1000%，彻底无法平A
- **一枝独秀** (`yzdx.lua`)：性能优化，攻击时不再遍历 `AllPlayers`，改为每3秒缓存一次，高频攻击不再卡顿
- **烷基八氮** (`wjbd.lua`)：移除 0.5s `FindEntities` 轮询，改 `onthaw` 事件驱动冰爆
- **我就跟着混** (`genzhe.lua`)：队友检测改 5s 间隔 + 玩家加入/离开事件刷新；掉落监听改 `entity_death` 事件
- **草莓奶昔** (`strawberry.lua`)：粒子特效加 3 秒冷却
- **咕咕咕** (`gugugu.lua`)：鸽子反击改用粒子特效代替乌鸦实体
- **蝴蝶的小阿飞** (`hufei.lua`)：蝶翼特效加 2 秒冷却
- **萝的守护** (`luo.lua`)：闪避特效加 2 秒冷却
- **是萌新喵** (`mxm.lua`)：卸载时恢复原始 `PushEvent`
- **酸酸草** (`suansuancao.lua`)：酸性伤害改用持久定时器，不再每次攻击取消重建
- **打工人** (`dagongren.lua`)：日夜检测改 `WatchWorldState` 事件驱动
- **妖精庇护** (`fay.lua`)：夜间检测改 `WatchWorldState` 事件驱动
- **无欲无求** (`wywq.lua`)：空闲检测间隔 0.5s → 1s
- **心平气和** (`xping.lua`)：动物检测间隔 2s → 5s
- **千月野** (`qianyue.lua`)：夜间特效间隔 2s → 10s，动物检测 3s → 5s，萤火虫不再重复生成
- **九月** (`jiuyue.lua`)：树木扫描间隔 3s → 10s

## 1.14.2 - 2026-06-21

### 新增
- **附魔：七步之外** — 词条ID `Legend_CHANGPI`（#29）
  - 攻击距离增加 2~4（随机词条值 `value_range`），支持多件叠加
  - 获取：受到 100 点以上的伤害时 1% 概率获得附魔石（`healthdelta` 事件监听，稳定生效）

### 修复
- **劳动最光荣** (`laodong.lua`)：
  - 修复：快采、快速制作、 烹饪秒出锅不生效的问题

## 1.14.1 - 2026-06-21

### 修复
- **胖虎** (`panghu.lua`)：移除魔音贯耳的眩晕/打断逻辑（`GoToState("hit")` + `SetTarget(nil)`），避免实体死亡后状态切换导致的引擎级崩溃。附魔改为纯范围伤害
- **劳动最光荣** (`laodong.lua`)：修复部署环境吃烤土豆计数器不生效的问题
  - 根因：135保底计数器被 `Moon_IsHHEnabled()` 守卫条件挡住，部署后 HH 检测失败导致计数器从未注册
  - 修复：将计数器移至文件顶层无条件注册，`HHSpawnStoneById` 调用加 `pcall` 兜底
- **劳动最光荣**：工作和制作速度增强
  - 快采：`workAddSpeed` 从 1（+100%）提升到 100（+10000%），真正秒完成
  - 秒制作：`buildingtime` 从 `×0.05`（20倍）改为固定 `0.001`，真正秒做
  - 烹饪秒出锅：由 `cooktimemult = 0.01` 改为 hook `StartCooking` 后 `cooktime = 0`，放入即出锅

### 修改
- **劳动最光荣** (`laodong.lua`)：描述文本同步更新为"秒采/秒制作/秒出锅"

## 1.14.0 - 2026-06-20

### 新增
- **附魔：劳动最光荣** — 词条ID `Legend_LDG`（#28）
  - 快采：采集/砍伐/挖掘速度+100%（HH框架 `workAddSpeed`)
  - 快速制作：制作速度+20倍（`buildingtime × 0.05`）
  - 烹饪秒出锅：放入食材即出锅（`cooktimemult = 0.01`）
  - 劳动有喜：每次完成工作/采摘有5%概率获得藏宝图（`hh_treasure_tally`）
  - 获取：吃烤土豆(`potato_cooked`)，135个保底获得附魔石
- **附魔：萝的守护** — 词条ID `Legend_LUO`（#25）
  - 50%几率免疫伤害，免疫僵直/击退，不会被影怪主动攻击
  - 每次攻击叠1层「守护」，每层+5%免伤，最多6层+30%，持续30秒
  - 掉落：精英/Boss 死亡 0.5% 概率掉落
- **附魔：良弓藏** — 词条ID `Legend_LIANGGONG`（#26）
  - 自身伤害-100%，每30秒自动射出一发无形箭矢，造成300%攻击力伤害
  - 掉落：精英/Boss 死亡 0.8% 概率掉落
- **附魔：怀民** — 词条ID `Legend_HUAIMIN`（#27）
  - 每晚入夜时怀民喊你起床，随机获得一个夜间祝福（伤害+50% / 移速+50% / 陪你走走+25%伤害），天亮清除
  - 掉落：精英/Boss 死亡 0.8% 概率掉落
- **快捷发送** — 助手面板新增"快捷发送"模块，输入文字后点击"发送"按钮快速聊天

### 修改
- **助手面板**：底部新增拖动手柄，面板过长时也可拖动
- **空白** (`kongbai.lua`)：重做数值
  - 触发几率：30% → **60%**
  - 每层伤害加成：+25% → **+60%**
  - 持续时间：10秒 → **30秒**（满层+300%）
  - 移除免疫冰冻/眩晕/击退效果（回归纯清除增益定位）
- **摸瓜吃鱼** (`mgcy.lua`)：修复 `Vector3` 未加 `_G.` 前缀导致的崩溃
- **enchants.md**：重写为规范的 Markdown 表格格式

### 新增配置
- `ENABLE_QUICK_CHAT` — 快捷发送开关，默认开启

## 1.13.9 - 2026-06-20

### 修改
- **酸酸草** (`suansuancao.lua`)：重做为「酸蚀叠层」体系，对齐其他1%掉落附魔强度
  - 新增「酸蚀」叠层：连续攻击同一目标叠层（1/2/3层），防御降低 **30%→35%→40%**，持续8秒
  - 满3层额外每秒10%攻击力的酸性伤害
  - 酸味食物回复量翻倍→**3倍**，加速 **15%→25%**，持续 **10→12秒**
  - 吃酸味食物后下一次攻击**必叠2层**（快速启动）
  - 加速期间攻击附带**2%最大生命酸性伤害**（每3秒一次）

### 修复
- **灵尾印记** (`myxl_level.lua`)：百宝囊附魔检查只匹配了 `myxl_bag3x3`，遗漏 `myxl_bag3x4`～`myxl_bag3x7`
  - 修复：改为 `string.find(inst.prefab, "myxl_bag3x")` 前缀匹配，兼容所有百宝囊尺寸

## 1.13.8 - 2026-06-20

### 新增
- **附魔：君可知** — 词条ID `Legend_JUNJUN`
  - 受击限伤不超过最大生命15%，触发限伤后获得0.5秒无敌帧
  - 每次触发限伤叠1层「铁壁」：+5%减伤，持续15秒（最多6层=30%）
  - 无敌结束时对周围6码敌人造成攻击力200%范围伤害（内置冷却5秒）
  - 掉落：精英/Boss 死亡 1% 概率掉落

### 修改
- **哎哟** (`aiyo.lua`)：重做为「以牙还牙」
  - 回血：50%（延迟一帧）→ **60%（瞬发）**，可救命
  - 反弹：80% → **100%**
  - 新增「记仇」：被攻击时标记攻击者，对其伤害+20%，持续10秒
  - 新增「报仇」：击杀被标记的敌人时回15%最大生命+周围5码150%冲击波

### 修复
- **跟着混** (`genzhe.lua`)：修复 `Vector3` 未加 `_G.` 前缀导致的崩溃 — 沙盒环境下 `Vector3` 为 nil
- **胖虎** (`panghu.lua`)：修复 `FindEntities` 参数传反 — 小动物标签放在 `mustHaveOneOfTags` 而非 `mustNotHaveTags`

## 1.13.7 - 2026-06-18

### 新增
- **附魔：等秋零** — 词条ID `Legend_DENGQIULING`
  - 属性：攻击力 +25%，移动速度 +15%
  - 效果：受到攻击时 20% 几率完全免疫伤害并回复 15 点精神值，触发台词「浅笑依然痴若离」与落叶特效
  - 掉落：精英/Boss 死亡 1% 概率掉落

### 修复
- **附魔隔离崩溃修复**：修复由于隔离沙箱环境下部分全局 Lua 函数未添加 `_G.` 前缀引发的 `pcall` 试图调用空值错误
  - **胖虎** (`Legend_PANGHU`)：修复攻击触发魔音贯耳时因 `pcall` 导致的崩溃错误
  - **打工人** (`Legend_DGR`)、**我就跟着混** (`Legend_GENZHE`)、**摸瓜吃鱼** (`Legend_MGCY`)：同步修复潜在的 `pcall` 裸调用崩溃隐患

## 1.13.6 - 2026-06-18

### 修复
- **掉落限流误伤**：`DropLoot` 覆盖逻辑对所有非堆叠物品无条件截断到 `MAX_NON_STACKABLE`，导致非垃圾的合法战利品被静默丢弃
  - 修复：非堆叠分支增加 `IsJunkLoot` 判断 — 垃圾物品（蓝图/卷轴等）仍截断，非垃圾物品全部生成
- **安全补丁 `TUNING` 引用**：`security_patch.lua` 中 `TUNING` 缺少 `_G.` 前缀，与其他文件不一致，极端沙箱环境下可能崩溃

### 修改
- **快捷自杀**：关闭聊天指令监听（`#zs`/`#kill`/`#自杀`），聊天消息会先广播后执行导致泄露到公屏；保留面板按钮触发

## 1.13.5 - 2026-06-18

### 修复
- **附魔重装失效**：修复所有附魔装备卸下后重新装备，效果不生效的问题
  - 根因：`un_equip_fn` 清理钩子/任务后未重置 `owner._xxx_hooked` 标记，导致 `on_equip_fn` 重装时跳过钩子安装
  - 涉及 18 个附魔：番茄炒蛋、无欲无求、哎哟、妖精庇护、干饭人、跟着、咕咕咕、蝴蝶小阿飞、九月、空白、篮球、摸瓜吃鱼、是萌新喵、千月野、草莓奶昔、烷基八氮、心平气和、一枝独秀
  - `Moon_AddEffect` 计数器正常恢复，但实际功能（定时任务、DoDelta 包装、事件监听）因标记未清除而全部丢失

## 1.13.4 - 2026-06-18

### 修复
- **是萌新喵**：修复玩家死亡时 `SetInvisible` 方法不存在导致的崩溃
  - `playervision:SetInvisible()` 并非 DST 标准 API，替换为 `inst:Hide()` / `inst:Show()`

## 1.13.3 - 2026-06-17

### 优化
- **蝴蝶的小阿飞**：特效优化
  - 抗伤特效：蝴蝶实体 → `statue_transition_2` 粒子光效，无需延时删除
  - 击杀回复特效：2只蝴蝶实体 → 1个粒子光效，且只在真正恢复蝴蝶时生成（满3只时不触发，避免连击视觉泛滥）
  - 击杀回复逻辑：修改为仅在 `_hufei_butterflies < 3` 时才执行 +1 和特效，满时仅回血回精神

## 1.13.2 - 2026-06-17

### 修改
- **灵尾印记**：新增百宝囊(`myxl_bag3x3`)附魔支持，现在背包和百宝囊均可附魔
- **蝴蝶的小阿飞**：攻击→防御重做
  - 移除攻击触发蝶翼飞刃（有无限蝴蝶 bug）
  - 改为最多 3 只蝴蝶护体，受到伤害时消耗 1 只蝴蝶完全抵消伤害
  - 每 8 秒自动恢复 1 只蝴蝶，击杀恢复 1 只（上限 3）
  - 保留移速 +20%、击杀回复 15 血 +10 精神
- **云中雀**：暂时注释 modmain 中的注入，待后续重新启用

## 1.13.1 - 2026-06-16

### 新增
- **13 个新附魔石**，全部 Boss 1% 掉落：

| 附魔 | ID | 效果简述 |
|------|-----|---------|
| 一枝独秀 | `Legend_YZDX` | 有托托莉=1%噩梦伤害，否则=1%血量撕裂+8%吸血+50%增强；solo翻倍 |
| 无欲无求 | `Legend_WYWQ` | 站立2秒入禅定：每秒回5%三维+减伤80%+免疫仇恨 |
| 烷基八氮 | `Legend_WJBD` | 攻击20%冰冻2秒，解冻冰爆300%AoE+减速，对冰冻+50%伤害 |
| 篮球 | `Legend_LANQIU` | 同目标连击+10%/层(10层)，满层灌篮500%暴击，换目标减半 |
| 哎哟 | `Legend_AIYO` | 受伤回50%血量+反弹80%伤害给攻击者 |
| 妖精庇护 | `Legend_FAY` | 攻击附带8%已损HP真伤，击杀回30精神，夜间+20%移速+夜视，15%减速 |
| 云中雀 | `Legend_YZQ` | 移速+35%，每8秒翱翔：下次攻击350%AoE+无敌1.5秒 |
| 摸瓜吃鱼 | `Legend_MGCY` | 击杀50%额外掉落，吃鱼回复翻倍+10秒50%增伤，采集+100% |
| 空白 | `Legend_KONGBAI` | 攻击30%清目标buff，免控，每清一个+25%伤害(5层) |
| 草莓奶昔 | `Legend_STRAWBERRY` | 每3秒回5%血+3%精神，被击使攻击者减速35%，甜食+20血 |
| 是萌新喵 | `Legend_MXM` | 致命伤留1血+隐身3秒(CD90s)，对高血量+60%伤害，首击必暴300%，不掉落 |
| 咕咕咕 | `Legend_GUGUGU` | 闪避率+42%，闪避成功=暴击+400%(6秒)，失败=3只鸽子反击200% |
| 干饭人 | `Legend_GANFAN` | 进食速度+200%，每吃食物叠干劲(8%伤害+5%移速，8层)，满层冲击波250%AoE |
| 蝴蝶的小阿飞 | `Legend_HUFEI` | 攻击25%释放3只蝶翼飞刃各120%伤害，击杀回15血+10精神，移速+20% |
| 千月野 | `Legend_QIANYUE` | 娱乐属性：夜晚星光萤火虫特效+微弱照明，小动物跟随，满月随机变身 |

### 优化
- **紫蝶强化**：冷却 480s→300s，复活后新增「蝶变」15秒（+50%伤害+30%移速+无敌），冷却期间被动+10%移速
- **附魔石背景色统一**：除紫蝶保留紫色外，其余全部统一为淡红色 `{1, 0.4, 0.4, 1}`

### 修复
- **安全补丁**：拦截 3273001012 拒绝商店外的非法购买
- 修复毛旭附魔反复脱穿装备刷血 bug：脱下时保存当前血量，重新装备时恢复，不再无条件回 200 血

## 1.13.0 - 2026-06-15

### 新增
- **附魔：番茄炒蛋** — 词条ID `Legend_FQCD_SANITY`
  - 描述：san值恢复速度大幅提升,san值消耗减半,每5秒回复20点san
  - san恢复8倍 | san消耗-50% | 每5秒+20san
  - 对比窥秘(5倍/消耗-30%/无回san)全面更强
  - 掉落：精英/Boss 死亡 3% 概率掉落
  - 颜色：番茄红


- **附魔：紫蝶** — 词条ID `Legend_ZD_BUTTERFLY`
  - 描述：蝶破茧，人不灭
  - 效果：受到致命伤害时免疫死亡，满血复活，冷却1天（480秒）
  - 唯一生效（多件不叠加免死次数）
  - 掉落：精英/Boss 死亡 3% 概率掉落
  - 颜色：紫色
- 新增 `Moon_HasEffect` 工具函数


- **附魔：毛旭** — 词条ID `Legend_MX_HEALTH`
  - 描述：geigei,带我吃鸭蛋
  - 效果：穿戴时人物血量上限+200，同时回复200血，脱下时扣除
  - 支持多件装备叠加（每件+200）
  - 掉落：精英/Boss 死亡 3% 概率掉落
  - 颜色：橙色

### 修复
- 重构附魔注册守卫条件：毛旭不再依赖璇儿 Mod 启用，仅需 HH 附魔框架
